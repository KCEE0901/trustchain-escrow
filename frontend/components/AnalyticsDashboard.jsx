'use client';

export default function AnalyticsDashboard() {
  return (
    <section aria-label="Analytics dashboard">
      <button type="button" aria-label="Refresh analytics dashboard">
        Refresh
      </button>
    </section>
  );
}
